import type { BrowserWindow } from "electron";

export function setMainOption(mainWindow: BrowserWindow) {
  // 设置
  // mainWindow.setOverlayIcon(nativeImage.createFromPath("path/to/overlay.png"), "Description for overlay");

}
